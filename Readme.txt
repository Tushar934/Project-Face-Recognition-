I am uploading the three files 
1. recognition.py
2. Training.py
3. detector.py

1. recognition.py contains the python code which explains--------------------------------------- 
that the user captures the image from the system camera and stores in the images folder.

2. Training.py contains the python code which explains------------------------------------------ 
that  in the prevoius program(recognition.py) that the user captures the image which will be train to the model.

3.detector.py contains the python code which explains-------------------------------------------
that after train the model detect the images tht the user captures.